import { CustomHomeFilterPipe } from './custom-home-filter.pipe';

describe('CustomHomeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomHomeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
