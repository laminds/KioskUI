import { CustomMemberFilterPipe } from './custom-member-filter.pipe';

describe('CustomMemberFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomMemberFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
