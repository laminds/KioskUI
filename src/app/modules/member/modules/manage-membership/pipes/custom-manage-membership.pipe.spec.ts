import { CustomManageMembershipPipe } from './custom-manage-membership.pipe';

describe('CustomManageMembershipPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomManageMembershipPipe();
    expect(pipe).toBeTruthy();
  });
});
