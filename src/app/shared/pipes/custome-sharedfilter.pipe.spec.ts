import { CustomeSharedfilterPipe } from './custome-sharedfilter.pipe';

describe('CustomeSharedfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomeSharedfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
